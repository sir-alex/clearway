import React from 'react';

import styles from './styles/index.module.scss';

export const ArtistAdd: React.FC = React.memo(() => {
    return (
        <h3 className={styles.txt}>Add artist</h3>
    )
})
